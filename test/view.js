
function removeAll(selectBox) {
    while (selectBox.options.length > 0) {
        selectBox.remove(0);
    }
}
  
  function addOption(selectBox, label, value) {
  
  }
  
  function init_dropdown() {
    //read yaml file, build document list
    //init drop down according to doc list
    //select current document
    //this func should be added as handler for selection change, and when a document is selected by double click.
  
  }
  
  function select_document(){
  
  }
  
  //will be called whenever something changed in the editor
  function on_change(){
  
  
  }
  
  function update_view() {
    //update view according model.
  
  }
  